<h1>Welcome#index</h1>

<%= form_for @post do |f| %>
  <%= f.text_field :title %>
  <%= f.file_field :image %>
<% end %> 


<% @posts.each do |post| %>
   <%= post.title %>
   <%= image_tag "data:image/jpeg;base64,#{post.image}" %>
<%end %>